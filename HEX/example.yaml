schemaVersion: 3
meta:
  sourceVersionId: de02fb56-bd3d-40d2-9d4e-ac268323b3e2 # DO NOT CHANGE - Hex uses this to match up project versions when reimporting the file
  description: ""
  projectId: 34b33fe3-74d4-492e-9749-0779ff5419bd # DO NOT CHANGE - Unique ID of the project from which this file was generated
  title: example
  timezone: null
  appTheme: SYS_PREF
  codeLanguage: PYTHON
  status: null
  categories: []
  castDecimalsDefault: true
  logicQueryCacheTimeout: null
  publishedQueryCacheTimeout: null
  hexType: PROJECT
  allowExecutionReordering: true
  prerunApp: false
  cachePublishedAppState: true
  refreshStalePublishedApp: false
  autoRerunApp: true
projectAssets:
  dataConnections: []
  envVars: []
  secrets: []
sharedAssets:
  secrets: []
  vcsPackages: []
  dataConnections: []
  externalFileIntegrations: []
cells:
  - cellType: CODE
    cellId: 075dbe03-c826-44af-bce9-d30bfa6ea20a # DO NOT CHANGE - Hex uses this to match up cells when reimporting the file, and detect any changes to existing cells
    cellLabel: null
    config:
      source: |-
        import snowflake.connector
        import pandas as pd
        from snowflake.connector import DictCursor

        conn = snowflake.connector.connect(
            user="manthan",
            password="Manthanrana123",
            account="ls31517.ap-southeast-1",
            role="ACCOUNTADMIN",
            warehouse="COMPUTE_WH",
            database="SNOWLENS",
            schema="DEMO",
        )
        cur = conn.cursor(DictCursor)

        query = 'select * from "QUERY_HISTORY_TABLE"'
        cur.execute(query)
        data_fetch = cur.fetchall()
        df = pd.DataFrame(data_fetch)
        print(df)

        cur.close()
        conn.close()
  - cellType: CODE
    cellId: 4f8339c6-ac81-4fa4-aded-3f4423c69029 # DO NOT CHANGE - Hex uses this to match up cells when reimporting the file, and detect any changes to existing cells
    cellLabel: null
    config:
      source: |-
        import snowflake.connector
        import pandas as pd
        from snowflake.connector import DictCursor

        conn = snowflake.connector.connect(
            user="manthan",
            password="Manthanrana123",
            account="ls31517.ap-southeast-1",
            role="ACCOUNTADMIN",
            warehouse="COMPUTE_WH",
            database="SNOWLENS",
            schema="DEMO",
        )
        cur = conn.cursor(DictCursor)

        query = 'select * from "QUERY_HISTORY_TABLE"'
        cur.execute(query)
        data_fetch = cur.fetchall()
        df = pd.DataFrame(data_fetch)
        print(df)

        cur.close()
        conn.close()
appLayout:
  visibleMetadataFields:
    - NAME
    - DESCRIPTION
    - AUTHOR
    - LAST_EDITED
    - LAST_RUN
    - CATEGORIES
    - STATUS
    - TABLE_OF_CONTENTS
  fullWidth: false
  tabs:
    - name: Tab 1
      rows:
        - columns:
            - start: 0
              end: 120
              elements:
                - showSource: false
                  hideOutput: false
                  type: CELL
                  cellId: 4f8339c6-ac81-4fa4-aded-3f4423c69029
                  sharedFilterId: null
                  height: null
                  showLabel: true
sharedFilters: []
